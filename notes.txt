Layout en todas las páginas

-Para usar el mismo layout en todas las páginas, iremos al archivo _app.js.
-Aquí, el elemento <Component /> debe estár envuelto en ese layout.
-Al guardar los cambios, no se verán hasta reiniciar el servidor.