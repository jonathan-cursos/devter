Rutas dinamicas

-Para la creación de rutas dinamicas, dentro de pages, colocaremos el nombre de la 
  carpeta que será la página, y para definir una ruta dinamica usaremos [nombre_ruta].
-Ese [nombre_ruta] puede ser el nombre de una carpeta, o un archivo js.
-Usaremos el archivo en caso de que no tengamos más rutas adentro de esa.