Correccion de autenticación:
  -Como en el index estamos diciendo que mostramos el boton de GH solo sí el valor
    del usuario es null, pero el valor por defecto es undefined, solo mostrará el
    boton sí hacemos una corrección en la función que mantiene activa la sessión.
  -El cambio debe ser, que el valor de normalizedUser, sí no traemos un usuario,
    sea null.